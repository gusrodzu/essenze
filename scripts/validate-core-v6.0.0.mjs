import fs from 'node:fs';const s=fs.readFileSync('apps/api/prisma/schema.prisma','utf8'),a=fs.readFileSync('apps/api/src/routes/masterData.js','utf8'),w=fs.readFileSync('apps/web/src/pages/MasterData.jsx','utf8');for(const x of ['model MasterDataContact','model MasterDataAddress','model MasterDataCatalog','model DataQualityIssue'])if(!s.includes(x))process.exit(1);for(const x of ['/dashboard','/quality/scan','/entities','DUPLICATE','INCOMPLETE'])if(!a.includes(x))process.exit(1);for(const x of ['<KpiGrid>','<KpiCard>','<KpiInfo'])if(!w.includes(x))process.exit(1);console.log('OK: BuzzBee Core v1 Master Data validado.');