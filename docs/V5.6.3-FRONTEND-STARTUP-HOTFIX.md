# v5.6.3 — Frontend startup hotfix

La pantalla `Iniciando BuzzBee…` pertenece al HTML estático de `index.html`.

React no la reemplazaba porque `navigation.js` usaba:

```js
icon: CreditCard
```

sin importar `CreditCard` desde `lucide-react`.

Se agregó:

```js
import {
  ...
  CreditCard,
  ...
} from 'lucide-react';
```

No hay cambios de esquema ni datos.
