# v0.8.0 — Almacenes

## Alcance

- Directorio de almacenes.
- Relación obligatoria con sucursal.
- Código único por sucursal.
- Estados activo e inactivo.
- Auditoría y permisos.

## API

- `GET /api/warehouses`
- `POST /api/warehouses`
- `PUT /api/warehouses/:id`
- `PATCH /api/warehouses/:id/status`
- `DELETE /api/warehouses/:id`
